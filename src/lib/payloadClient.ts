/*
 * Dead code — the queryPayload() function posted to /api/payload which was never
 * implemented. Removed during the Phase 1 cleanup. If a client-side query utility
 * is needed in the future, implement the API route first, then add the client here.
 *
 * The original type helpers (PopulateFields, WhereOperators, etc.) were not
 * referenced elsewhere, so they were removed as well.
 */
